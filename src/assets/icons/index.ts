import addIcon from "./add.svg"
import previewIcon from "./preview.svg"
import deleteIcon from "./delete.svg"
import importIcon from "./import.svg"

export const iconArray = [addIcon, previewIcon, importIcon, deleteIcon]

export default {
    addIcon,
    previewIcon,
}